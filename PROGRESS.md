# Voidfront progress

## 2026-09-03 19:39 America/Los_Angeles — first development run

Baseline: empty checkout, no existing implementation, STOP or completion marker. Initialized local Git on dev. No development remote is configured. Verified Godot 4.7.2.stable.official.ed1daf0bf, matching template directory, MSVC VS2022 Professional and Blender 5.1.2. Hardware queries via CIM were denied; CPU registry reports Ryzen 7 3700X, GPU/RAM remain unverified.

Concrete goals: (1) independent fixed-tick C++ simulation with canonical command/replay tests; (2) Godot C++ bridge and controllable 3D skirmish; (3) original Blender walker with baked animations, import/export and actual packaged runtime evidence. Full lockstep transport, complete economy, advanced pathing and production content are later milestones, not claims for this run.

First increment: independent C++ simulation now supports canonical bounded commands, integer grid movement/reservations, group destination assignment, immediate Stop/Hold, attack-move/combat, deterministic skirmish AI and explicit state hashes. Standalone MSVC Debug/Release tests pass. 2,000-tick traces match across configurations and ten Release repeats (SHA256 B5FBB540C91632A5FDF71952A3E8C6B315526AC3C500FE5F7E171C2ACECFD1B1). Independent critic found receipt-time dead-unit rejection; fixed with early/late future-command regression. See sim/README.md for exact limitations and preliminary timings. No network transport or full economy exists.

Original Blender walker created and structurally validated: editable source, reproducible generator/export, 15 bones, five baked clips, five material surfaces, 8,888 triangles. Runtime inspection is pending client integration.

Integrated Debug C++ bridge now compiles and its sim tests pass. godot-cpp API profile requires OS in addition to RefCounted for upstream print_string.cpp. This host needs PATH/Path normalization and single MSBuild node; scripts encode both. Client runtime/export work is in progress. Detailed results and exact continuation will be recorded before the run marker is released.

## 2026-09-03 20:10 America/Los_Angeles — packaged foundation checkpoint

Landed a controllable 3D six-versus-six skirmish: click/drag/Shift selection, F2 army select, right-click Move, A+click AttackMove, immediate S/H Stop/Hold, camera pan/zoom, team health bars, minimap, projectile feedback, unit deaths, outcome display and restart. Godot interpolates integer simulation snapshots; no Godot navigation or physics determines movement/combat. Enemy uses the same canonical simulation command interface. This combat bot is not a complete RTS AI.

Pinned Godot 4.7.2/template binary hashes and godot-cpp SHA/API now verified in a running Release package. Editable Blender source and GLB integrate with five clips. All source build/import/export/capture steps are scripted. The package and play instructions are at `C:\dev\voidfront\artifacts\package\Voidfront.exe` and `PLAY.md`. Engine console/editor paths and tool pins remain in tools/toolchain.json. No development remote exists; checkpoints have local Git protection only.

Added persistent VFR1 canonical-input recording/playback, bounds/compatibility checks and a first-divergence trace comparison tool. `tools/verify.ps1` passes integrated MSVC Debug/Release CTest, distinct record/playback invocations, fifteen malformed replay fixtures per configuration, cross-configuration 2,000-tick equality and ten repeat traces. Saved inputs/hash traces are in artifacts/determinism. These are file replays; no network transport exists yet.

Final `tools/capture.ps1 -Packaged -Name packaged-smoke -Ticks 400` passes click, drag, movement, stop without drift, attack-move, combat, imported/requested animation clips and restart. End hash b559cd93ea152fc0. Normal capture wall-clock frame interval p95/p99 17.075/17.503 ms; bridge step p95/p99 0.046/0.084 ms; resident peak 231,268,352 bytes. Twelve-unit test only; frame p99 misses the 16.67 ms target. Test machine is Ryzen7 3700X/RTX2070 SUPER/~64GB, not the reference target. Headless 200/500-unit results and measurement caveats are in TESTING.md. No budget was weakened.

Recorded a 20.033-second, 1202-frame animation movie, converted to artifacts/packaged-animation.mp4 and inspected extracted walk/combat/overview sequences. Movie writer resolution is 1600x900; screenshots are 1920x1080. Basic limb motion, orientation and subdued wrecks are visible without obvious detached limbs; attack recoil, hit response, complete death collapse and animation transitions need closer inspection. Encoding-distorted movie timings are excluded from performance evidence.

Critics: independent simulation reviewer found receipt-time dead-unit admission and weak smoke assertions; both fixed and re-reviewed. Independent visual reviewer found washed-out terrain, confusing corpses, poor outcome contrast and unbounded telemetry; fixed and screenshots re-reviewed. Runtime testing additionally found synthetic input stretch/dispatch mistakes and GLES3 material lifetime errors on restart; coordinate/event assertions and cached materials fixed them. Final logs contain only the known sandbox root-certificate-store diagnostic. Reports live in .voidfront-agent/review-{simulation,visual}-2026-09-03.md. Official SC2 comparison media was inaccessible; no direct side-by-side approval is claimed.

Remaining gaps: actual lockstep/network impairment tests, executed-state/content checksums, full economy/building/tech/fog, complete AI/1v1 matches, queued commands/control groups/enemy context targeting, scalable crowd/dynamic blocker pathing, manual input-latency evidence, production terrain/art/animation/audio/VFX/UI, reference hardware profiling and balance. Unit surfaces/selection rings still overbright. This is an early playable foundation, not AAA or completion evidence; no COMPLETE.md created.

**Exact next step:** introduce deterministic per-player tick frames and an executed-state checksum distinct from receive buffers. Run two separate Windows processes through actual transport, stall on missing frames, reject incompatible content/protocol, report timeout/desync and replay the resulting match trace. Add controlled latency/jitter/loss validation before attaching network play to the Godot client. Then extend the same command stream to economy and construction. Keep the existing skirmish working and preserve all source/art/evidence.

## 2026-09-03 21:37 America/Los_Angeles - network foundation run

Baseline: clean dev at a64808d, previous run marker released, STOP/COMPLETE absent. Origin is now configured at https://github.com/shoejunk/voidfront.git and dev tracks origin/dev; verify remote before pushing. Existing skirmish and all evidence are preserved.

Concrete goals: closed per-player tick frames with receive-independent executed-state hashes; actual separate Windows peer processes using bounded transport and compatibility/desync/disconnect diagnostics; deterministic latency/jitter/loss harness, cross-configuration replay evidence and independent code/evidence review. Client network UI and 30-minute complete matches remain subsequent gates.


## 2026-09-06 10:22 America/Los_Angeles - recover network validation

Baseline: clean dev at 96e56a9. Prior task 01a06ab2-5cf4-7653-b5be-d30a8a59d8db is verified idle with a usage-limit failure; reclaimed its run marker exclusively. STOP/COMPLETE absent. Network implementation was saved but the verification handoff is unfinished.

Goals: build the preserved closed-frame/UDP implementation in Debug and Release, run actual separate-process impairment and replay tests, fix demonstrated failures, independently review source and executed evidence, and publish a clean recoverable checkpoint. No client networking or full-match claim is implied.

First saved increment, 24b4842: independent reviewer found that the headless trace path could alias a replay/record through a hard link or Windows case variant. Disposable pre-fix regression reproduced accepted replay/hardlink output; source now checks filesystem identity and Windows case before opening files. Seven preservation cases pass in each configuration (exact/hardlink/case for replay and record, plus nonexistent case-aliased record outputs). Full integrated MSVC Debug/Release builds, 2/2 CTest each, fifteen malformed replays per configuration, 2,000-tick cross-configuration comparison and ten repeated replays pass. Evidence: artifacts/output-aliases-before.log and artifacts/replay-alias-verification-2026-09-06.log.

Network foundation now has executed evidence: eleven separate-process UDP cases pass after fixing the Python relay's fatal handling of Windows connection-reset notifications. Clean Debug/Release, mixed 80/160 ms RTT with 20 ms jitter and 1% loss run 1,000 ticks each; all traces and opposite-configuration replays agree, as do ten repeated playbacks. Forced terminal loss, missing frame/ACK, incompatible setup, desync and disconnect pass; failed 25-tick prefixes replay. Final evidence is isolated at artifacts/network-verified-2026-09-06/summary.json with per-case PIDs, reports, actual impairments, recordings/traces and executable fingerprints. Random drops: 155 at 80 ms, 207 at 160 ms. Detailed commands/results and trace SHA256 are in TESTING.md. Default future runs now create unique evidence directories, explicit reused paths reject, and case failures print immediately instead of waiting silently behind other workers.

Packaged local smoke also passes (artifacts/packaged-network-regression.json): existing selection/move/stop/combat/restart and hash b559cd93ea152fc0 preserved. Both screenshots actually inspected by primary and independent runtime critic. Current twelve-unit observations: frame interval p95/p99 3.036/3.611 ms, bridge-only advance 0.036/0.056 ms, resident peak 235,040,768 bytes. Different presentation/scheduling conditions prevent an optimization claim; representative battle and input-latency evidence remain missing. Independent network/runtime reports are .voidfront-agent/review-{network,runtime}-2026-09-06.md. Visual debt includes overbright walkers, crowded selection cues, blockout terrain and ambiguous defeat text; no new visual or shipping approval.

**Exact next step:** replace the network harness's stop-and-wait turn exchange with scheduled input delay and a bounded pipeline, decoupling future commands from tick-tagged lagged executed-state checksums. Preserve this suite and demonstrate sustained 20 Hz plus actual response timing at 80/160 ms RTT before attaching a nonblocking session to two packaged Godot clients. Current 1,000-tick cases take 143.52/220.83 seconds at 80/160 ms, so playable network timing remains unmet. Complete economy/AI, 30-minute matches, scalable pathing, production content/performance and shipping gates are still open. No COMPLETE.md; automation remains active.

Published checkpoints: 24b4842 (replay alias protection) and 6e6a201 (reviewed network verification/handoff) are pushed to origin/dev. Final metadata records that source checkpoint; run marker is released after a clean handoff commit. Automation remains active.

## 2026-09-06 - user playtest requirement: any-angle pathfinding

User reported rigid orthogonal movement and explicitly required any-angle movement with fast, efficient, optimal or near-optimal paths while leaving the strategy open. Roadmap now makes this a required part of the complete-small-match milestone before production content expansion. SPEC distinguishes the temporary grid prototype from the required final behavior. Acceptance includes independent shortest-path comparisons with a declared quality tolerance, existing performance/response budgets, deterministic group replanning, robust crowd movement and packaged playtest evidence. Documentation change only; current game movement is unchanged and no algorithm has been selected.

## 2026-09-06 11:47 America/Los_Angeles - pipelined lockstep run

Baseline: clean dev at c40d9cd, STOP/COMPLETE and prior run marker absent. Prior automation task is idle; exclusively acquired this task's marker. Preserve the user's any-angle pathfinding requirements and the existing local skirmish.

Goals: replace stop-and-wait input exchange with a bounded scheduled pipeline and independently tagged executed-state checksums; measure 20 Hz pacing and generated-command-to-execution delay under 80/160 ms RTT with jitter/loss; preserve negative protocol, missing-data, disconnect, terminal-confirmation and replay evidence. Independent source/evidence review is required before checkpoint. Client networking, manual input latency, complete matches and pathfinding remain open.

First pipeline increment verified: protocol 2 future input frames, independent executed-state checksum exchange, bounded 16-state verification lag, ACK-independent advancement, applied-prefix replay preservation and detailed timing evidence. Integrated Debug/Release builds, CTest 2/2 each, legacy replay/alias regressions and packaged offline smoke pass. Initial process probe exposed a 16 Hz Windows timer problem; scoped timer resolution and absolute deadlines corrected clean pacing to approximately 20 Hz. The complete 15-case, 1,000-tick isolated suite passes protocol/replay checks at artifacts/pipeline-verified-2026-09-06/summary.json. The 80 ms RTT case still misses generated-command p95 (163.7463/131.9515 ms): source tick 0 includes initial buffer warmup. This is a saved functional checkpoint with an explicit timing gap, not client multiplayer acceptance. Next bounded correction in this run: establish initial-buffer readiness before accepting any canonical input, expose setup duration, and repeat the full suite without filtering or retiming samples.

Readiness correction verified: first input is now accepted only after both initial empty buffers have been received and local frames acknowledged. Setup remains visible; source-zero command samples are retained. Final 16-case isolated UDP suite passes at artifacts/pipeline-ready-verified-2026-09-06/summary.json, including held startup ACK, held frame/checksum, lost ACKs, terminal loss/desync, incompatible setup and disconnect. Four 1,000-tick profiles and ten repeat replays agree (trace SHA256 b4aac2b20ec6ec7ae028341a6488abbd00938e62a3e00b3d6f0c19561a8d0776). At 80 ms RTT with 20 ms jitter/1% loss, generated-command p95 is 102.558/101.880 ms and pace 19.99472/19.99979 Hz; 222 actual packets dropped. At 160 ms, pace remains 19.75528/19.76406 Hz, with 503/487 ms aggregate stalls and 399 actual drops. Targets are unchanged; human/client latency is still unmeasured.

Integrated Debug/Release builds and 2/2 CTest, legacy malformed replay/alias checks, 2,000-tick cross-configuration traces and ten repeats pass. Final net-only readiness changes were rebuilt/tested in both configurations. Packaged offline 400-tick smoke and unchanged b559cd93ea152fc0 hash pass; both current screenshots independently inspected. Frame interval p99 17.412 ms still misses 16.67 ms; bridge p95/p99 0.048/0.070 ms and resident peak 242,659,328 bytes are twelve-unit observations. Overbright walkers, blockout terrain, crowded selection and ambiguous defeat text remain. No new motion/manual/networked-client proof. Independent reports are .voidfront-agent/review-pipeline-2026-09-06.md and review-runtime-pipeline-2026-09-06.md; bounded evidence acceptance only.

**Exact next step:** test a larger agreed input delay for the 160 ms RTT profile against its own same-delay clean baseline, preserving 80 ms responsiveness and visible setup timing. Then extract the headless loop into a reusable nonblocking session adapter for two packaged Godot clients, with explicit readiness/stall/disconnect/outcome UI and true input-to-execution/display measurements. Preserve the required any-angle pathfinding milestone, all existing art/source, and all full-match/performance/shipping gates. No COMPLETE.md; the weekly automation remains active.
